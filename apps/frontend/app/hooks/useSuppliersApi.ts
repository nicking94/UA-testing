// hooks/useSuppliersApi.tsimport { useState, useCallback } from 'react';import { suppliersApi } from '@/app/lib/api/suppliers';import { Supplier } from '@/app/lib/types/types';export const useSuppliersApi = () => {  const [suppliers, setSuppliers] = useState<Supplier[]>([]);  const [loading, setLoading] = useState(false);  const [error, setError] = useState<string | null>(null);  const fetchSuppliers = useCallback(async () => {    setLoading(true);    setError(null);    try {      const data = await suppliersApi.getAll();      setSuppliers(data);      return data;    } catch (err) {      const errorMessage = err instanceof Error ? err.message : 'Error al cargar proveedores';      setError(errorMessage);      console.error('Error fetching suppliers:', err);      throw err;    } finally {      setLoading(false);    }  }, []);  const fetchSupplierById = useCallback(async (id: number) => {    setLoading(true);    setError(null);    try {      const supplier = await suppliersApi.getById(id);      return supplier;    } catch (err) {      const errorMessage = err instanceof Error ? err.message : 'Error al cargar proveedor';      setError(errorMessage);      console.error('Error fetching supplier by ID:', err);      throw err;    } finally {      setLoading(false);    }  }, []);  const addSupplier = useCallback(async (supplier: Omit<Supplier, 'id'>) => {    setLoading(true);    setError(null);    try {      const newSupplier = await suppliersApi.create(supplier);      setSuppliers((prev) => [...prev, newSupplier]);      return newSupplier;    } catch (err) {      const errorMessage = err instanceof Error ? err.message : 'Error al crear proveedor';      setError(errorMessage);      throw err;    } finally {      setLoading(false);    }  }, []);  const updateSupplier = useCallback(    async (id: number, updates: Partial<Supplier>) => {      setLoading(true);      setError(null);      try {        const updated = await suppliersApi.update(id, updates);        setSuppliers((prev) =>          prev.map((s) => (s.id === id ? updated : s))        );        return updated;      } catch (err) {        const errorMessage = err instanceof Error ? err.message : 'Error al actualizar proveedor';        setError(errorMessage);        throw err;      } finally {        setLoading(false);      }    },    []  );  const deleteSupplier = useCallback(async (id: number) => {    setLoading(true);    setError(null);    try {      await suppliersApi.delete(id);      setSuppliers((prev) => prev.filter((s) => s.id !== id));    } catch (err) {      const errorMessage = err instanceof Error ? err.message : 'Error al eliminar proveedor';      setError(errorMessage);      throw err;    } finally {      setLoading(false);    }  }, []);  return {    suppliers,    loading,    error,    fetchSuppliers,    fetchSupplierById,    addSupplier,    updateSupplier,    deleteSupplier,    setSuppliers,  };};