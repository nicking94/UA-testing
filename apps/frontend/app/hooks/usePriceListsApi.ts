// hooks/usePriceListsApi.tsimport { useState, useCallback } from 'react';import { priceListsApi } from '@/app/lib/api/price-lists';import { PriceList } from '@/app/lib/types/types';export const usePriceListsApi = () => {  const [priceLists, setPriceLists] = useState<PriceList[]>([]);  const [loading, setLoading] = useState(false);  const [error, setError] = useState<string | null>(null);  const fetchPriceLists = useCallback(async () => {    setLoading(true);    setError(null);    try {      const data = await priceListsApi.getAll();      setPriceLists(data);      return data;    } catch (err: unknown) {      const errorMessage = (err as Error).message || 'Error al cargar listas de precios';      setError(errorMessage);      console.error('Error fetching price lists:', err);      throw err;    } finally {      setLoading(false);    }  }, []);  const fetchPriceListById = useCallback(async (id: number) => {    setLoading(true);    setError(null);    try {      const priceList = await priceListsApi.getById(id);      return priceList;    } catch (err: unknown) {      const errorMessage = (err as Error).message || 'Error al cargar lista de precios';      setError(errorMessage);      console.error('Error fetching price list by ID:', err);      throw err;    } finally {      setLoading(false);    }  }, []);  const addPriceList = useCallback(async (priceList: Omit<PriceList, 'id'>) => {    setLoading(true);    setError(null);    try {      const newPriceList = await priceListsApi.create(priceList);      setPriceLists((prev) => [...prev, newPriceList]);      return newPriceList;    } catch (err: unknown) {      const errorMessage = (err as Error).message || 'Error al crear lista de precios';      setError(errorMessage);      throw err;    } finally {      setLoading(false);    }  }, []);  const updatePriceList = useCallback(    async (id: number, updates: Partial<PriceList>) => {      setLoading(true);      setError(null);      try {        const updated = await priceListsApi.update(id, updates);        setPriceLists((prev) =>          prev.map((pl) => (pl.id === id ? updated : pl))        );        return updated;      } catch (err: unknown) {        const errorMessage = (err as Error).message || 'Error al actualizar lista de precios';        setError(errorMessage);        throw err;      } finally {        setLoading(false);      }    },    []  );  const deletePriceList = useCallback(async (id: number) => {    setLoading(true);    setError(null);    try {      await priceListsApi.delete(id);      setPriceLists((prev) => prev.filter((pl) => pl.id !== id));    } catch (err: unknown) {      const errorMessage = (err as Error).message || 'Error al eliminar lista de precios';      setError(errorMessage);      throw err;    } finally {      setLoading(false);    }  }, []);  return {    priceLists,    loading,    error,    fetchPriceLists,    fetchPriceListById,    addPriceList,    updatePriceList,    deletePriceList,    setPriceLists,  };};