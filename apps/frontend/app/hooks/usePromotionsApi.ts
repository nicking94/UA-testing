// hooks/usePromotionsApi.tsimport { useState, useCallback } from 'react';import { promotionsApi } from '@/app/lib/api/promotions';import { Promotion } from '@/app/lib/types/types';export const usePromotionsApi = () => {  const [promotions, setPromotions] = useState<Promotion[]>([]);  const [loading, setLoading] = useState(false);  const [error, setError] = useState<string | null>(null);  const fetchPromotions = useCallback(async () => {    setLoading(true);    setError(null);    try {      const data = await promotionsApi.getAll();      setPromotions(data);      return data;    } catch (err: unknown) {      const errorMessage = (err as Error).message || 'Error al cargar promociones';      setError(errorMessage);      console.error('Error fetching promotions:', err);      throw err;    } finally {      setLoading(false);    }  }, []);  const addPromotion = useCallback(async (promotion: Omit<Promotion, 'id'>) => {    setLoading(true);    setError(null);    try {      const newPromotion = await promotionsApi.create(promotion);      setPromotions((prev) => [...prev, newPromotion]);      return newPromotion;    } catch (err: unknown) {      const errorMessage = (err as Error).message || 'Error al crear promoción';      setError(errorMessage);      throw err;    } finally {      setLoading(false);    }  }, []);  const updatePromotion = useCallback(    async (id: number, updates: Partial<Promotion>) => {      setLoading(true);      setError(null);      try {        const updated = await promotionsApi.update(id, updates);        setPromotions((prev) =>          prev.map((p) => (p.id === id ? updated : p))        );        return updated;      } catch (err: unknown) {        const errorMessage = (err as Error).message || 'Error al actualizar promoción';        setError(errorMessage);        throw err;      } finally {        setLoading(false);      }    },    []  );  const deletePromotion = useCallback(async (id: number) => {    setLoading(true);    setError(null);    try {      await promotionsApi.delete(id);      setPromotions((prev) => prev.filter((p) => p.id !== id));    } catch (err: unknown) {      const errorMessage = (err as Error).message || 'Error al eliminar promoción';      setError(errorMessage);      throw err;    } finally {      setLoading(false);    }  }, []);  return {    promotions,    loading,    error,    fetchPromotions,    addPromotion,    updatePromotion,    deletePromotion,    setPromotions,  };};