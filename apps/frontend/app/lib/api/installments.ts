// lib/api/installments.tsimport { apiClient } from './client';import { Installment } from '../types/types';export interface InstallmentFilters {  creditSaleId?: number;  status?: string;  customerId?: string;}export const installmentsApi = {  async getAll(filters?: InstallmentFilters): Promise<Installment[]> {    const params = new URLSearchParams();    if (filters?.creditSaleId) params.append('creditSaleId', filters.creditSaleId.toString());    if (filters?.status) params.append('status', filters.status);    if (filters?.customerId) params.append('customerId', filters.customerId);    const query = params.toString();    return apiClient.get<Installment[]>(`/installments${query ? `?${query}` : ''}`);  },  async getById(id: number): Promise<Installment> {    return apiClient.get<Installment>(`/installments/${id}`);  },  async create(data: Omit<Installment, 'id'>): Promise<Installment> {    return apiClient.post<Installment>('/installments', data);  },  async createMany(data: Omit<Installment, 'id'>[]): Promise<Installment[]> {    return apiClient.post<Installment[]>('/installments/bulk', data);  },  async update(id: number, data: Partial<Installment>): Promise<Installment> {    return apiClient.put<Installment>(`/installments/${id}`, data);  },  async markAsPaid(id: number, paymentData: { paymentDate: string; paymentMethod: string }): Promise<Installment> {    return apiClient.put<Installment>(`/installments/${id}/pay`, paymentData);  },  async delete(id: number): Promise<void> {    return apiClient.delete<void>(`/installments/${id}`);  },};