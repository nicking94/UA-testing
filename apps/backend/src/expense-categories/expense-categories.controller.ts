import {  Controller,  Get,  Post,  Put,  Delete,  Body,  Param,  Query,  UseGuards,} from '@nestjs/common';import { ExpenseCategoriesService } from './expense-categories.service';import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';@Controller('expense-categories')@UseGuards(JwtAuthGuard)export class ExpenseCategoriesController {  constructor(private expenseCategoriesService: ExpenseCategoriesService) {}  @Get()  findAll(@Query() query: any) {    return this.expenseCategoriesService.findAll(query);  }  @Get(':id')  findOne(@Param('id') id: string) {    return this.expenseCategoriesService.findOne(+id);  }  @Post()  create(@Body() data: any) {    return this.expenseCategoriesService.create(data);  }  @Put(':id')  update(@Param('id') id: string, @Body() data: any) {    return this.expenseCategoriesService.update(+id, data);  }  @Delete(':id')  delete(@Param('id') id: string) {    return this.expenseCategoriesService.delete(+id);  }}