import {  Controller,  Get,  Post,  Put,  Delete,  Body,  Param,  Query,  UseGuards,} from '@nestjs/common';import { CustomCategoriesService } from './custom-categories.service';import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';@Controller('custom-categories')@UseGuards(JwtAuthGuard)export class CustomCategoriesController {  constructor(private customCategoriesService: CustomCategoriesService) {}  @Get()  findAll(@Query() query: any) {    return this.customCategoriesService.findAll(query);  }  @Get(':id')  findOne(@Param('id') id: string) {    return this.customCategoriesService.findOne(+id);  }  @Post()  create(@Body() data: any) {    return this.customCategoriesService.create(data);  }  @Put(':id')  update(@Param('id') id: string, @Body() data: any) {    return this.customCategoriesService.update(+id, data);  }  @Delete(':id')  delete(@Param('id') id: string) {    return this.customCategoriesService.delete(+id);  }}